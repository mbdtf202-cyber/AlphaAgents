# AA-SANDBOX-REVISION-002

This sandbox package proves the revision path: QA pass, buyer requests one bounded revision, seller resubmits, QA passes, then buyer accepts.

It is sample_only + sandbox_verified, not validated commercial evidence.
