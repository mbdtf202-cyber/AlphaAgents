# AA-SANDBOX-TRIAL-001

This is the canonical sandbox evidence package for the default first-purchase Trial path.

It is `sandbox_verified`, not `validated`.

Use it to verify:

- required package files exist;
- JSON objects use canonical state fields;
- evidence index has at least the Trial minimum evidence count;
- finance ledger uses deterministic conditional-release math;
- event sequence matches the contract golden path;
- UI, CLI, and API order snapshots are identical.
- buyer audit steps can sample topics and trace every sampled EvidenceRef back to `09-evidence-index.csv`.

Do not use it as proof of real payment, customer demand, or PMF.
